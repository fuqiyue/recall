# 项目接入流程（新项目与存量项目）

`recall init` 只负责 Git 管道（仓库、身份、自动同步、hook）。本文档回答它不回答的问题：
**logic_readme.md 的内容从哪来** —— 尤其是已有大量代码的存量项目。

**核心策略：模块化渐进接入。** 不做一次性全量扫描；先建根骨架，之后按模块补全。
文档补全只在两种时机发生：

1. **接入时（新项目开始使用 Recall）**：建立根骨架 + 主流程初稿（见下"第一步"）
2. **用户单独要求时**：用户点名某个模块或功能，才补全对应条目

此外，按 SKILL.md 核心原则 6（docs_impact）：修改触及尚未登记的模块时，在同一获授权
变更中顺带补齐该模块的登记条目——这是既有规则的自然结果，不是额外流程。

**不允许的做法**：为"补文档"擅自创建未登记的子级 logic_readme（违反 INV-001 与核心
原则 3；合法拆分见下"模块拆分"——必须经用户确认并登记）；未经用户确认就把 AI 推断
的功能意图标注为已确认。

---

## 第一步：根骨架（接入时，一次性）

在 `recall init` 之后，按 [logic_readme 模板](logic-readme-template.md) 建立**最小**根文档：

1. **文档控制 + 目标与边界**：项目是什么、负责什么、不负责什么（问用户，一句话级别）
2. **范围登记表**：列出顶层模块目录，每行只需 `module_id`、`scope_path`、`status`；
   尚未补全文档的模块登记为 `status: pending-docs`，让"哪里还没有文档"本身可见
3. **代码地图**：只填已识别的顶层入口（主入口文件、配置、测试目录），职责一句话；
   AI 从代码扫描得出的描述在"权威来源/决策依据"处标 `code-derived`，与用户确认的条目区分
4. **功能意图层初稿**：访谈式提问从用户口中提取（不从代码反推）：
   - "这个项目的用户是谁？他们用它完成什么？"（→ 3~8 条 INT）
   - "用户从打开到完成一件事的典型顺序是什么？"（→ 1~3 条 FLOW）
   - "哪些操作习惯绝对不能破坏？"（→ UXI，可为空）
5. **不变量与当前限制**：只写用户明确说出的约束，宁缺毋滥

产出以用户确认为生效条件：骨架落盘前把 INT/FLOW 初稿念给用户听，确认后写入。

## 第二步：模块补全（按触发时机，每次一个模块）

对触发补全的模块 `<scope_path>`：

1. **扫描**：AI 读该模块代码与测试，起草代码地图行（路径、职责、输入/输出、关联测试）
2. **访谈**：向用户确认两件事——该模块服务哪个用户目标（复用/新增哪条 INT）、
   位于哪条 FLOW 的哪一步；用户说不清时按核心原则 5 记为待澄清项，不硬填
3. **落盘**：用户确认后，在根文档中：
   - 范围登记表该行 `pending-docs` → `active`，需要时加稳定锚点小节（`<a id="scope-...">`）
   - 代码地图加行；INT/FLOW/UXI 增改对应条目（编号用 `INT-YYYYMMDD-NNN` 完整格式）
4. **验证**：`recall validate`（意图层引用与锚点检查）+ `python scripts/audit_logic_map.py . --current-state`

单模块补全属于中等变更以下的文档工作，不要求 CHG/VER；一次性补全多个模块或
借机调整制度时按三条通道正常分流。

## 模块拆分（子 logic 文档，RULE-018）

项目大到单文件低效时，把复杂模块的正文拆成子文档。**拆分由 AI 建议、用户决定**：
AI 在审查或修改中发现触发信号时提出建议并说明理由，用户确认后才实施。

**触发信号**（示例，非硬阈值）：根文档接近行数上限（目标 250 / 硬 400）且压缩后仍不足；
某模块的规则与 INT 条目高度聚集（如占全文三分之一以上）；模块有独立的发布节奏或团队边界。

**拆分步骤**：

1. 用户确认拆分哪个模块
2. 在模块目录创建 `<scope_path>/logic_readme.md`：沿用 [logic_readme 模板](logic-readme-template.md)，
   但不含 root-only 字段（coverage_policy 等）与范围登记表；`module_id`/`scope_path`/`parent`
   指向自身与根文档，`module_doc_policy: readme-only`，`canonical_change: none`
3. 根范围登记表该模块行 `doc_policy` 改为 `readme-only`，`logic_readme` 列直接链接子文档
   （不再需要根内锚点），`logic_change` 列填 `none`
4. 把该模块的规则、代码地图行与 INT/FLOW 条目迁入子文档；根文档保留全局规则、
   跨模块契约与该模块的路由行（一句话职责即可）
5. 引用旧根锚点（`#scope-...`）的活跃 CHG 改为引用 `MOD-ID`；跑
   `python scripts/audit_logic_map.py . --current-state` 确认路由对账通过

**拆分后的阅读与修改顺序**：改子模块 = 先读根文档（总规章 + 路由），再读该模块子文档；
跨模块变更 = 读全部相关子文档，并在同一变更中一起更新。根规章优先于子文档。
`logic_change.md` 与 `logic_version/` 不拆分，全项目始终只有根一份。

## 存量项目的节奏建议

- 接入当天只做第一步，控制在半小时内；价值从第一次"改代码前先读文档"就开始兑现
- 优先补全的模块 = 近期要改的模块（git log 热点），而不是按目录字母序
- `pending-docs` 长期存在是正常状态：没被改动的模块不欠文档
