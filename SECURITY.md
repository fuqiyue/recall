# Security Policy

## 支持的版本

当前正在接收安全更新的版本：

| 版本 | 支持状态 |
| --- | --- |
| 0.2.x | :white_check_mark: |
| 0.1.x | :white_check_mark: |
| < 0.1 | :x: |

## 报告漏洞

如果你发现了安全漏洞，请**不要**在公开的 Issue 中报告。

### 报告流程

1. **私下联系**
   - 通过 GitHub Security Advisories: [创建私密报告](https://github.com/fuqiyue/recall/security/advisories/new)
   - 或发送邮件至项目维护者（如果已公开联系方式）

2. **提供信息**
   - 漏洞描述
   - 复现步骤
   - 影响范围
   - 可能的修复方案（如果有）

3. **响应时间**
   - 我们会在 **48 小时内**确认收到报告
   - 在 **7 天内**提供初步评估
   - 根据严重程度制定修复计划

### 安全注意事项

Recall 主要处理本地文件和 Git 仓库，但仍需注意：

#### 对用户的建议

- **不要将敏感信息写入决策记录**
  - 避免在 `logic_version/records/` 中记录密码、API 密钥
  - 使用环境变量或配置文件的路径引用，而非实际值

- **审查提交内容**
  - 使用 `git diff` 检查即将提交的内容
  - 特别注意 `logic_change.md` 和新创建的 VER 记录

- **权限控制**
  - 如果 Recall 仓库包含业务逻辑决策，考虑设置适当的访问权限
  - 私有仓库用于敏感项目

#### 已知限制

- **Git 历史不可变性**: 一旦提交到 Git，历史记录难以完全删除
- **文件权限**: Recall 不加密存储，依赖文件系统权限
- **命令注入**: CLI 工具执行 Git 命令，请只在可信环境使用

### 安全最佳实践

开发者在使用 Recall 时：

```bash
# ✅ 好的做法：引用配置，不暴露值
echo "数据库连接字符串配置在 .env 文件中" > logic_change.md

# ❌ 避免：直接写入敏感信息
echo "数据库密码是 MySecretPass123" > logic_change.md
```

### 感谢

我们感谢负责任地披露安全问题的研究人员，并会在修复后公开致谢（如果你同意）。

---

**记住**：安全是共同责任。如有疑问，请先联系我们！
